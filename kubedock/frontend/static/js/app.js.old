var KubeDock = new Backbone.Marionette.Application({
    regions: {
        contents: '#contents'
    }
});

KubeDock.on('start', function(){
    if (Backbone.history) {
        Backbone.history.start({root: '/'});
    }
});

