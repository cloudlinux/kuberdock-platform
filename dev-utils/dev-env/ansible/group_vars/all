add_testing_repo: "{{ '-t' if (testing_repo is defined and testing_repo) else '' }}"
non_floating_ips_mode: "{{ '--nonfloating-ip' if (non_floating_ips is defined and non_floating_ips) else '' }}"
src_prefix: /var/opt/kuberdock
is_dev_install: false
